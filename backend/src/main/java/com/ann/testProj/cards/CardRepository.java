package com.ann.testProj.cards;

// Импортируем JpaRepository — это готовый интерфейс из Spring Data JPA,
// который даёт нам стандартные методы для работы с базой данных.
import org.springframework.data.jpa.repository.JpaRepository;

/** 2
 * 🧩 CardRepository — это интерфейс, который отвечает за работу с базой данных.
 *
 * Мы не пишем в нём код вручную — всё делает Spring за нас.
 *
 * 💡 Задача репозитория:
 *    хранить, искать, изменять и удалять объекты типа Card в базе данных.
 *
 * 📘 Интерфейс наследуется от JpaRepository<Card, Long>:
 *    - первый параметр `Card` → с каким типом данных работает (наша сущность)
 *    - второй параметр `Long` → тип поля ID (у нас id — это Long)
 *
 * После этого Spring автоматически создаёт реализацию этого интерфейса,
 * и мы можем использовать его методы прямо в контроллерах:
 *
 * ✅ repo.findAll()        — получить список всех карточек
 * ✅ repo.findById(1L)     — найти карточку по id
 * ✅ repo.save(card)       — сохранить или обновить карточку
 * ✅ repo.deleteById(1L)   — удалить карточку
 *
 * 📊 Проще говоря:
 * Это "мост" между нашим Java-кодом и базой данных.
 * Мы обращаемся к базе как к обычному объекту — без SQL.
 */
public interface CardRepository extends JpaRepository<Card, Long> {
    // Мы не добавляем сюда код — JpaRepository уже всё умеет.
    // Но при необходимости можно дописать свои методы, например: List<Card> findByStatus(String status);
}
